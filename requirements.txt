# WorthIt.gg pipeline. Python >= 3.10 (google-genai 2.x drops 3.9).
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
#
# google-genai 2.x is required, not optional: 1.x has no thinking_level on
# ThinkingConfig, only the legacy thinking_budget that Gemini 3.x replaced.
google-genai>=2.15.0
requests>=2.32.0

# IANA time zone database. Quota accounting keys on midnight Pacific (Google
# resets RPD there, not at UTC midnight), and zoneinfo needs system tzdata or
# this package. Pure-python, no service, no cost.
tzdata
